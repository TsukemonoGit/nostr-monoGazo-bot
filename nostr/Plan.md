- kind30003購読をやめる。
- add でnostr:nevent nostr:noteや、nevent note などを受付可能にする。既存のJSONを渡すやり方をやめる。アプリ側で対象のノートを取得、必要な情報を取得する。また、add nevent~~~ -n 1 ？とか？でnevent内に複数の画像がある場合の番号を指定できるようにする。（していなくて、複数画像がある場合は既存のどれにしますか？で処理する部分を流用。）
